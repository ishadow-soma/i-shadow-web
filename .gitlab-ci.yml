stages:
    - test
    - build
    - deploy

test:
    stage: test
    script:
        - echo test

build:
    stage: build
    script:
        - echo test

deploy:
    stage: deploy
    script:
        - echo testest
        - ls /
        - ls /home
        - ls /home/ubuntu
        - cd /home/ubuntu/ishadow-react
        - git add .
        - git commit -m "build"
        - git pull
        - npm install
        - npm run build
